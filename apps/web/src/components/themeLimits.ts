/**
 * What a 主题 is allowed to be, in both clients.
 *
 * One line, this long. A 主题 is a subject, not a brief — the brief is 立言指令 —
 * and the server refuses anything longer, so both the workbench and the
 * extension bound the box rather than letting a writer discover the limit by
 * being refused.
 */
export const MAX_THEME_CHARACTERS = 80;
